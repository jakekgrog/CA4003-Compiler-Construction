var i : integer;

integer multiply (x:integer, y:integer)
{
    var result: integer;
    var minus_sign: boolean;
    // figure out sign of result and convert args to absolute values
    if (((8 + (6 + 5) == 8)) && (5 - (9 - 7) < (6 - 5) + 8) )
    {
        minus_sign = true;
        x = -x;
    }

    else
    {
        if y < 0 && x >= 0
        {
            minus_sign = true;
            y = -y;
        }
        else
        {
            if (x < 0) && y < 0
            {
                minus_sign = false;
                x = -x;
                y = -y;
            }
            else
            {
                minus_sign = false;
            }
        }
    }
    return (x);
} 

integer test_fn(x: integer)
{
    var i: integer;
    i = 0;
    while (i < 4 + 7 + 8 && true == true)
    {
        i = i + 1;
    }
    return();
}

main
{
    var arg1: integer;
    var result: integer;
    const five: integer = 5;
    var i: integer;
    arg1 = -6;
    arg2 = five;
    multiply(arg1, arg2);
    result = multiply(arg1, arg2);
    i = 1;
    i = testfn(i);
}