var test: boolean;
const myBool: integer = 5;


boolean less(x: integer, y: integer) {
    var isLess: boolean;
    if (x < y && y > x) {
        isLess = true;
    } else {
        isLess = false;
    }
    return(isLess);
}

main
{
    var x: integer;
    var y: integer;
    var z: integer;
    var i: boolean;

    test = true;
    x = 5;
    y = 5;

    i = less(x, y);
}